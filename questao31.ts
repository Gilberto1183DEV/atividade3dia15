const nomeProduto = prompt("Digite o nome do produto:");
const precoProduto = 100.00; // Exemplo de preço fixo, pode ser substituído pelo desejado
const simboloMoeda = "R$"; // Exemplo de símbolo de moeda, pode ser substituído pelo desejado
console.log(`Produto: ${nomeProduto}, Preço: ${simboloMoeda}${precoProduto}`);
